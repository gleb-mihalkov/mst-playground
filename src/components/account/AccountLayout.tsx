import { FC } from 'react';

/**
 * Отображает обрамление страницы в приватной части сайта (шапка, подвал,
 * контейнер содержимого).
 */
const AccountLayout: FC = ({ children }) => <>{children}</>;

export default AccountLayout;
